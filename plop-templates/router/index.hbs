import Layout from '@/layout/index.vue'
export default {
  path: '/{{ name }}',
  component: Layout,
  children: [
    {
      // 默认路由,空字符串
      path: '',
      name: '{{ name }}',
      component: () => import('@/views/{{ name }}/index.vue'),
      meta: { title: '{{ title }}' }
    }
  ]
}
